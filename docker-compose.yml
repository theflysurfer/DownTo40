version: '3.8'

services:
  app:
    build: .
    container_name: energie-40eur-dashboard
    ports:
      - "8508:8501"
    restart: unless-stopped
    environment:
      - TZ=Europe/Paris
    volumes:
      - ./results:/app/results:ro
      - ./data:/app/data:ro
